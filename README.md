Random Forest succeeds: It naturally handles mixed feature types, automatically learns non-linear decision boundaries, and builds in feature selection at each split — so it down-weights irrelevant features like race and native_country without any manual tuning. The ensemble of 200 trees also dramatically reduces variance compared to a single tree. The feature importance table confirms the model latches onto the genuinely predictive signals: capital_gain, relationship (a proxy for marital/household status), and education_num — all intuitively connected to income.